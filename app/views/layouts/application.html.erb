<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    
    <%= render "layouts/rails_default" %>
    <%= render "layouts/shim" %> <!-- app/views/layouts/_shim.html.erbというファイルを探しにいって挿入する -->
    
  </head>
  <body>
    
    <%= render "layouts/header" %>
    
    <div class="container">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
      <%= yield %>
      <%= render "layouts/footer" %>
      <%= debug(params) if Rails.env.development? %>   <!-- Rails.env.development?で開発環境でのみデバック情報が表示される -->
    </div>
  </body>
</html>